package org.firstinspires.ftc.teamcode.lib;

public enum GrabberState {
    /**
     * closed = touching a block
     * open = fully open; ready for a block
     * partial = ready to grab a block
     */
    CLOSED,OPEN,PARTIAL
}
